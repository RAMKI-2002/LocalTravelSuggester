# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32

# Data validation / config
pydantic>=2.10
pydantic-settings>=2.7
python-dotenv>=1.0

# HTTP client (async)
httpx>=0.27

# Database
# 2.0.41+ fixes a typing bug that breaks on Python 3.14.
SQLAlchemy>=2.0.41
# psycopg v3 - works on Python 3.13/3.14 with a prebuilt binary wheel.
psycopg[binary]>=3.2

# AWS
boto3>=1.35

# Retries
tenacity>=9.0

# Testing
pytest>=8.3
pytest-asyncio>=0.24
respx>=0.22
